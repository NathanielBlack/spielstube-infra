---
registry_domain: registry.spielstube.app
registry_user: deploy
# Override these with ansible-vault or --extra-vars
registry_password: changeme
# Shared bearer token the registry sends to Watchtower on push notifications.
# Real value lives in inventory/vault.yml.
watchtower_http_token: changeme
